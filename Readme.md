This is a web page with a "canvas" on which things can be drawn by moving
the cursor over its squares; this action paints the squares with a random color.
The user is also allowed to generate a new canvas of a size of their choosing by 
pressing a button and writing their choice in the prompt.
The page was made using HTML, CSS, and JavaScript.